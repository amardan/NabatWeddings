## 2026-05-22 - Native Lazy Loading HTML
**Learning:** For static sites, using native HTML `loading="lazy"` is often better and simpler than writing custom IntersectionObserver JS for images.
**Action:** When working on simple static sites without complex bundlers, look for easy native HTML attribute optimizations before jumping to JS solutions.
